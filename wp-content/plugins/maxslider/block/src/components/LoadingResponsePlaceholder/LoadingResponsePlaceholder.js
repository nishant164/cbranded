import { Spinner } from 'wp.components';

const LoadingResponsePlaceholder = () => {
  return (
    <div className="maxslider-loading-response-placeholder">
      <Spinner />
    </div>
  );
};

export default LoadingResponsePlaceholder;
